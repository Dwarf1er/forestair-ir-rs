# Add local cargo bin to PATH
export PATH="$(pwd)/.esp/bin/bin:$PATH"

# Source local espup environment variables
if [ -f "$(pwd)/.esp/espup/export-esp.sh" ]; then
    source "$(pwd)/.esp/espup/export-esp.sh"
fi
